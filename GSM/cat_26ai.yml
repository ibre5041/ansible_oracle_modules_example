---

- hosts: all
  collections:
    - ibre5041.ansible_oracle_modules
  become: true
  become_user: root
  become_method: sudo
  vars:
    password: AcrorA123456__
    oracle_db_ver_path: 23.26.1.0.0
    oracle_home: "{{ oracle_install_dir_prod }}/{{ oracle_db_ver_path }}"
    oracle_sid: "CATALOG"

  any_errors_fatal: true

  roles:
    - role: default_vars_only
      tags: [always, default_vars_only]

  environment:
    ORACLE_HOME: "{{ oracle_home }}"
    ORACLE_SID:  "CATALOG"

  tasks:
    - name: Create wallet directory {{ item }}
      file:
        path: "{{ item }}"
        state: "{{ oracle_db_state | default('directory') }}"
        group: oinstall
        mode: '0700'
      loop:
        - /home/oracle/wallet.catalog
        - /home/oracle/wallet.catalog/tde
      become_user: "{{ oracle_os_user }}"
      become: true
      tags: [oracledb, dbca]
      
    - name: create database
      oracle_db:
        oracle_home: "{{ oracle_home }}"
        db_name: 'CATALOG'
        sid: 'CATALOG'
        db_unique_name: 'CATALOG_RW'
        sys_password: "{{ password }}"
        #
        flashback: true
        archivelog: true
        force_logging: true
        db_options:
          JSERVER: false
          ORACLE_TEXT: false
          IMEDIA: false
          CWMLITE: false
          SPATIAL: false
          OMS: false
          APEX: false
          DV: false
        initparams:
          sga_target: 4G
          sga_max_size: 4G
          wallet_root: '/home/oracle/wallet.catalog'
          tde_configuration: 'KEYSTORE_CONFIGURATION=FILE'
        storage_type: ASM
        datafile_dest: +XDATA
        recoveryfile_dest: +XDATA
        state: "{{ oracle_db_state | default('present') }}"
        cdb: true
      tags: [oracledb, dbca]
      become_user: "{{ oracle_os_user }}"
      become: true

    - name: Ensure oracle parameters are set
      oracle_parameter:
        mode: sysdba
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        state: present
        scope: "{{ item.scope }}"
        sid: '*'
      loop:
        - { name: wallet_root, value: '/home/oracle/wallet.catalog', scope: spfile }
        - { name: open_links, value: 30, scope: spfile }
        - { name: open_links_per_instance, value: 30, scope: spfile }
        - { name: standby_file_management, value: auto, scope: both }
        - { name: global_names, value: 'FALSE', scope: both }
        - { name: db_files, value: 1024, scope: spfile }
        - { name: local_listener, value: '(ADDRESS=(PROTOCOL=TCP)(HOST={{ ansible_hostname }})(PORT=1521))', scope: both }        
      tags: [oracledb, keystore]
      become_user: "{{ oracle_os_user }}"
      become: true
      register: oracle_parameter_results

    - name: restart database
      oracle_db:
        oracle_home: "{{ oracle_home }}"
        db_name: 'CATALOG'
        db_unique_name: 'CATALOG_RW'
        #sys_password: "{{ password }}"
        state: "{{ oracle_db_state | default('restarted') }}"
      tags: [oracledb]
      become_user: "{{ oracle_os_user }}"
      become: true
      when: oracle_parameter_results.changed

    - name: Create a pdb PCATALOG
      oracle_pdb:
        mode: sysdba
        #
        pdb_name: "PCATALOG"
        state: present
        open: true
        save_state: true
        pdb_admin_username: admin
        pdb_admin_password: "{{ password }}"
        roles: [CONNECT, PDB_DBA]
      tags: [oracledb, pdb]
      become_user: "{{ oracle_os_user }}"
      become: true
      
    - name: Ensure oracle parameters are set
      oracle_parameter:
        mode: sysdba
        name: tde_configuration
        value: "KEYSTORE_CONFIGURATION=FILE"
        state: present
        scope: both
        sid: '*'
      tags: [oracledb, keystore]
      become_user: "{{ oracle_os_user }}"
      become: true
      
    - name: Create keystore
      oracle_wallet:
        mode: sysdba
        state: present
        auto_login: auto_login
        container: all
        keystore_password: "{{ password }}"        
      tags: [oracledb, keystore]
      become_user: "{{ oracle_os_user }}"
      become: true
      register: _

    - name: Open keystore
      oracle_wallet:
        mode: sysdba
        state: present
        open: true
        auto_login: auto_login
        container: all
        keystore_password: "{{ password }}"        
      tags: [oracledb, keystore]
      become_user: "{{ oracle_os_user }}"
      become: true
      register: _

    - name: Validate keystore configuration
      oracle_sql:
        mode: sysdba
        sql: |
          select wrl_type, wrl_parameter, status from v$encryption_wallet;
      register: keystore_status
      tags: [oracledb, keystore]
      become_user: "{{ oracle_os_user }}"
      become: true

    - name: Display keystore status
      debug:
        msg: "{{ keystore_status.data }}"
      tags: [oracledb, keystore]

    - name: Ensure gsmcatuser is unlocked and password is set
      oracle_user:
        mode: sysdba
        schema: gsmcatuser
        schema_password: "{{ password }}"
        state: present
        locked: false
        container: all
      tags: [oracledb, user]
      become_user: "{{ oracle_os_user }}"
      become: true

    - name: "{{ oracle_sid }} tnsnames.ora"
      oracle_tnsnames:
        path: "{{ oracle_home }}/network/admin/tnsnames.ora"
        alias: "{{ item.name }}"
        whole_value: "{{ item.value }}"
      loop:
        - { name: CATALOG,  value: '(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=gsm-shard-emea)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=CATALOG)))'}
        - { name: PCATALOG, value: '(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=gsm-shard-emea)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=PCATALOG)))'}
      tags: [files]
      become_user: "{{ oracle_os_user }}"
      become: true
